import { Typography } from '@mui/material';

const Footer = () => {

    return (
        <section>
            <Typography variant="caption">This app was made with React and Material-UI</Typography>
        </section>
    )
};

export default Footer;