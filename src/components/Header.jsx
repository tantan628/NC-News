import { Typography } from '@mui/material';

const Header = () => {

    return (
        <section>
            <Typography variant="h1" className="mainTitle">
                Tanis' News App
            </Typography>
        </section>
    )
};

export default Header;